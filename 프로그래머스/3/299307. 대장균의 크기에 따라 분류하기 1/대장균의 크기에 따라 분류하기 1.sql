-- 코드를 작성해주세요
SELECT ID, "LOW" as SIZE
FROM ECOLI_DATA
WHERE SIZE_OF_COLONY <= 100

union

SELECT ID, "MEDIUM"
FROM ECOLI_DATA
WHERE SIZE_OF_COLONY > 100
AND SIZE_OF_COLONY <= 1000

union

SELECT ID, "HIGH"
FROM ECOLI_DATA
WHERE SIZE_OF_COLONY > 1000

ORDER BY ID;